/**
 * video.ts
 * video will use the port given by the redroid runner and that port has raw h246 shit in it (not exactly sure what format srry)
 * it will turn it into rtp with ffmpeg and given the socket its function was called with, it will basically pipe the data to the webrtc socket given
 */
