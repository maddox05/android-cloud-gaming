/**
 *
 * input.ts will take inputs bassed on X
 * it will be like a proxy to the actual srcpy shell inputs
 * so it will take in a input javascript obkect and convert it to the correct adb input.
 *
 * for srcpy it will open a port for input so we will use that port and inject inputs per srcpy specs
 *
 * the input object this file takes in will be as follows
 * input will take in the input type and translate it to scrcpy input
 */
